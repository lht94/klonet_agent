knowledge/klonet_source/** whitespace=-trailing-space,-space-before-tab,-blank-at-eof

# Shell 脚本强制 LF。CRLF 会让 Linux 上的 bash 报 `$'\r': command not found`
# 与 `set: pipefail: invalid option name` —— 实测在测试服务器上踩过。
*.sh text eol=lf
